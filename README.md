# Innovative IQ

Next.js + TypeScript single-page consultancy site recreated from the approved design.

## Local development

```bash
npm install
npm run dev
```

## Visual-review stage
The native `/api/contact` endpoint validates and sanitises submissions but deliberately does not send external email yet. Production email delivery is connected after visual approval, per project brief.
